/* Styles pour l'élément li */
li {
    font-size: 18px;
    color: #555;
    margin-bottom: 10px;
}

/* Styles pour l'élément nav */
nav {
    display: inline-block;
    width: 80%;
    background-color: #f5f5f5;
    padding: 15px;
    border-radius: 8px;
}

/* Styles pour l'image à l'intérieur de l'élément nav */
nav img {
    width: 40px;
    height: 40px;
    border-radius: 50%;
}

/* Styles pour la classe grid */
.grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
}

/* Styles pour les images à l'intérieur de la classe grid */
.grid img {
    width: 100%;
    height: auto;
    border-radius: 8px;
}

/* Styles pour la classe flex */
.flex {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
}

/* Ajoutez d'autres styles au besoin */
